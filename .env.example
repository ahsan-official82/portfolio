# Gmail SMTP — use an App Password (Google Account → Security → 2-Step Verification → App passwords)
# Copy to .env.local and fill in real values (never commit .env.local)

SMTP_USER=ahmer.official92@gmail.com
SMTP_PASS=uqdw zeju fokc ltgy

# Inbox that receives contact form submissions (defaults to SMTP_USER if omitted)
CONTACT_TO=ahmer.official92@gmail.com
